This is a Laravel package used for learning how to c reate a package

HOW TO USE
>>>> Setup database, email and set the value of ADMIN_EMAIL in .env file
>>>> Publish config file with the command "php artisan vendor:publish --tag=contactform-config"
>>>> Run migration with the command "php artisan migrate"